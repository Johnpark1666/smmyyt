{
  "name": "summarizer-sync",
  "version": "1.0.0",
  "private": true,
  "description": "SMMYNP 웹 뷰어 + 동기화 API (Cloudflare Worker + R2)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}